//你现在是棒球比赛记录员。 
//给定一个字符串列表，每个字符串可以是以下四种类型之一： 
//1.整数（一轮的得分）：直接表示您在本轮中获得的积分数。 
//2. "+"（一轮的得分）：表示本轮获得的得分是前两轮有效 回合得分的总和。 
//3. "D"（一轮的得分）：表示本轮获得的得分是前一轮有效 回合得分的两倍。 
//4. "C"（一个操作，这不是一个回合的分数）：表示您获得的最后一个有效 回合的分数是无效的，应该被移除。 
// 
//每一轮的操作都是永久性的，可能会对前一轮和后一轮产生影响。 
//你需要返回你在所有回合中得分的总和。 
//
// 示例 1: 
//
// 输入: ["5","2","C","D","+"]
//输出: 30
//解释: 
//第1轮：你可以得到5分。总和是：5。
//第2轮：你可以得到2分。总和是：7。
//操作1：第2轮的数据无效。总和是：5。
//第3轮：你可以得到10分（第2轮的数据已被删除）。总数是：15。
//第4轮：你可以得到5 + 10 = 15分。总数是：30。
// 
//
// 示例 2: 
//
// 输入: ["5","-2","4","C","D","9","+","+"]
//输出: 27
//解释: 
//第1轮：你可以得到5分。总和是：5。
//第2轮：你可以得到-2分。总数是：3。
//第3轮：你可以得到4分。总和是：7。
//操作1：第3轮的数据无效。总数是：3。
//第4轮：你可以得到-4分（第三轮的数据已被删除）。总和是：-1。
//第5轮：你可以得到9分。总数是：8。
//第6轮：你可以得到-4 + 9 = 5分。总数是13。
//第7轮：你可以得到9 + 5 = 14分。总数是27。
// 
//
// 注意： 
//
// 
// 输入列表的大小将介于1和1000之间。 
// 列表中的每个整数都将介于-30000和30000之间。 
// 
// Related Topics 栈 
// 👍 147 👎 0


package leetcode.editor.cn;

import java.util.Stack;

//Java：棒球比赛
public class P682BaseballGame{    
    public static void main(String[] args) {      
        Solution solution = new P682BaseballGame().new Solution();       
        // TO TEST
        String[] ops = {"5","-2","4","C","D","9","+","+"};
        solution.calPoints(ops);
    }
    //leetcode submit region begin(Prohibit modification and deletion)

class Solution {
    public int calPoints(String[] ops) {
        if (ops == null || ops.length <= 0) {
            return 0;
        }
        Stack<String> stack = new Stack<>();
        int count = 0;
        for (String op : ops) {
            if ("C".equals(op) && !stack.empty()) {
                count -= Integer.parseInt(stack.peek());
                stack.pop();
            } else if ("D".equals(op) && !stack.empty()) {
                count += Integer.parseInt(stack.peek()) * 2;
                stack.push(Integer.toString(Integer.parseInt(stack.peek()) * 2));
            } else if ("+".equals(op) && !stack.empty()) {
                // 先取第一轮的得分
                String str1 = stack.pop();
                // 计算前两轮的得分
                int countNow = Integer.parseInt(str1) + Integer.parseInt(stack.peek());
                count += countNow;
                // 将前一轮的得分从新push进栈中
                stack.push(str1);
                // push进这一轮的得分
                stack.push(Integer.toString(countNow));
            } else {
                count += Integer.parseInt(op);
                stack.push(op);
            }
        }
        return count;
    }
}
//leetcode submit region end(Prohibit modification and deletion)
/**
 * 解答成功:
 * 		执行耗时:3 ms,击败了86.99% 的Java用户
 * 		内存消耗:38.3 MB,击败了42.23% 的Java用户
 */
}